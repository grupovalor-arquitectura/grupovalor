import { Box } from "@mui/material";

export default function TimelineFuture() {
  return (
    <Box
      sx={{
        height: "100vh",
        backgroundColor: "rgba(255,0,0,.2)",
      }}
    />
  );
}